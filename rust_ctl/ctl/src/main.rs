// mod config;
// use crate::config::Config;

fn main() -> Result<(), String> {
    todo!("nothing is implemented")
}

// fn main() -> Result<(), String> {
//     let config = config::Config::from_args(env::args())?;
//     if let Some(action) = &config.action {
//         action.init()?.perform(&config)?;
//     }
// 
//     Ok(())
// }
